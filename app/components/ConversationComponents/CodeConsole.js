import React, { useState } from 'react';
import { useSelector, useDispatch } from 'react-redux';

export default ({ reply }) => {

  const runCode = () => {
    
  }

  return (
    <div>
      <iframe>

      </iframe>
    </div>
  )
}